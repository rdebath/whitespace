lll		quit		End

lsl	L	jmp		Jump
lss	L	label		Label
lst	L	call		Call

ltl		ret		Return
lts	L	jz		If-Zero
ltt	L	jn		If-Negative

sll		drop		Discard
sls		dup		Dup
slt		swap		Swap

ss	N	push		Push

// These two added later -- V0.3
// Pick (Copy/Ref) is just like Forth; "Slide" is a limited substitute for ROT.
// The "N" must be positive so only valid forms are "stls" and "stss"
stl	N	leave		Slide
sts	N	pick		Ref		0 pick == dup, like forth

tlss		outc		OutputChar	as forth emit
tlst		outn		OutputNum
tlts		readc		ReadChar	reads into memory like forth "expect" but only one char at a time.
tltt		readn		ReadNum

tssl		mul		Times		order as forth
tsss		add		Plus		order as forth
tsst		sub		Minus		order as forth
tsts		div		Divide		order as forth
tstt		mod		Modulo		order as forth

tts		store		Store		swaped relative to forth
ttt		fetch		Retrieve

// Unused prefixes. (ssl may be a zero)
lls
llt
ssl
stt
tll
tlsl
tltl
tsl
tstl
ttl
